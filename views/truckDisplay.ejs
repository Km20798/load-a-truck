<%- include('./parts/head') %>

    <%- include('./parts/nav') %>


    <div class="container">
        <div class="row">
            <form action="/trucks/<%= truck.id%>/getData" method="post">
                <div class="form-group">
                    <input type="datetime-local" name="search" id="time" class="form-control">
                    <button class="btn btn-success">click</button>
                </div>
            </form>
        </div>
    </div>


    <!-- <% x=truck.weight  , number=0%>
    <div class="container" style="margin-top: 20px;">
        <div class="row">
            <h5>Before Parcels</h5>
            <p>Number of Parcels : <%= number%></p>
            <p>weight of truck : <%= x %></p>
        </div>
    </div>
    <% parcels.forEach(parcel => { %>
        
       <div class="container" style="margin-top: 20px;">
           <div class="row">
               <%number=number+1%>
               <% x+=parcel.weight %>
            <h5>@<%= parcel.createdAt%></h5>
            <p>Number of Parcels : <%= number%></p>
            <p>weight of parcel : <%= parcel.weight %></p>
            <p>weight of truck : <%= x %></p>
           </div>
       </div>

    <% })%>
 -->


    
<%- include('./parts/footer') %>